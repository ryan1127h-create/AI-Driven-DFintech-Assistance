"""
Application tracker — reports where an applicant's own application stands
(submitted documents, outstanding requirements, decision status) and
notifies them as it changes, so "where's my application" has an answer
grounded in real application state rather than the admission-document
checklist alone.
"""
