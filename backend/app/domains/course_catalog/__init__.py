"""
Read-only browsing of the raw NUS course catalog (course code, title,
credits, prerequisites, ...) — the reference data behind the student-facing
curriculum page. Unrelated to course_recommendation, which scores and
selects a personalised subset of this same underlying data; this domain
just lists everything, unfiltered and unranked.
"""
