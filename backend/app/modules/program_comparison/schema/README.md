# program_comparison — database schema

This module is planned to be read-only over the shared knowledge base
(`app.document_chunks`, competitor_programs rows) and owns no tables yet.

If user comparison history ever needs persisting, add a
`student.program_comparisons` table here (per the integration plan §11).
