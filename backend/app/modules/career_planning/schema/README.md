# career_planning — database schema

This module is planned to be read-only (profile via profile.interface,
knowledge via the shared RAG service) and owns no tables yet.

If generated plans ever need persisting, add a `student.career_plans`
table here (per the integration plan §11).
