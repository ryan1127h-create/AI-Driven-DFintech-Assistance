# agent-backend 里"我的检索(rag_backend)"需要的配置。
# 用法:复制本文件为 .env,填入真实值。放在 agent-backend/rag_backend/.env

# --- 连数据库(Supabase PostgreSQL,检索的知识库在这) ---
DATABASE_URL=

# --- Embedding(把问题转成向量做检索) ---
OPENAI_API_KEY=

# --- Rerank(四层检索第四层,精排) ---
COHERE_API_KEY=
