# 数据库连接（所有脚本都要）
python-dotenv==1.0.1
psycopg[binary]==3.2.3

# Embedding：切片时把 chunk 转向量，检索时把问题转向量
openai==2.45.0

# Token 计数：B 类切片判断某一节超没超 500 token
tiktoken==0.13.0

# ② BM25 关键词检索（内存版，不需要 Elasticsearch）
rank-bm25==0.2.2

# ④ Rerank 精排
cohere==7.0.5

# PDF 文本提取（读 Annex A 等源文档时用）
pypdf==6.14.2
